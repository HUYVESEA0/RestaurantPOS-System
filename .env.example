# ========================================
# Restaurant POS System - Environment Variables
# ========================================

# ========================================
# DATABASE CONFIGURATION
# ========================================

# Option 1: SQL Server with Windows Authentication (Recommended for Development)
DATABASE_SERVER=\\SQLEXPRESS
DATABASE_NAME=RestaurantPOS
DATABASE_TRUSTED_CONNECTION=true
DATABASE_MULTIPLE_ACTIVE_RESULT_SETS=true
DATABASE_TRUST_SERVER_CERTIFICATE=true

# Full connection string (auto-constructed)
# Server={DATABASE_SERVER};Database={DATABASE_NAME};Trusted_Connection={DATABASE_TRUSTED_CONNECTION};MultipleActiveResultSets={DATABASE_MULTIPLE_ACTIVE_RESULT_SETS};TrustServerCertificate={DATABASE_TRUST_SERVER_CERTIFICATE}

# Option 2: SQL Server LocalDB (Alternative for Development)
# DATABASE_CONNECTION=Server=(localdb)\\mssqllocaldb;Database=RestaurantPOS;Trusted_Connection=true;MultipleActiveResultSets=true;TrustServerCertificate=true

# Option 3: SQL Server with SQL Authentication (Production)
# DATABASE_SERVER=your-server-name
# DATABASE_NAME=RestaurantPOS
# DATABASE_USERNAME=your-username
# DATABASE_PASSWORD=your-password
# DATABASE_TRUST_SERVER_CERTIFICATE=true

# Connection String (Constructed from above variables)
# Backend will use: "Server={DATABASE_SERVER};Database={DATABASE_NAME};Trusted_Connection={DATABASE_TRUSTED_CONNECTION};MultipleActiveResultSets={DATABASE_MULTIPLE_ACTIVE_RESULT_SETS}"

# ========================================
# JWT AUTHENTICATION CONFIGURATION
# ========================================

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
# Generate a secure key: openssl rand -hex 32
JWT_SECRET_KEY=your-very-secure-and-random-jwt-secret-key-here

# JWT Settings
JWT_ISSUER=RestaurantPOS.API
JWT_AUDIENCE=RestaurantPOS.Client
JWT_EXPIRY_HOURS=24

# ========================================
# EMAIL CONFIGURATION (SMTP)
# ========================================

# Email From Information
EMAIL_FROM_EMAIL=noreply@restaurantpos.com
EMAIL_FROM_NAME=Restaurant POS System

# SMTP Server Settings
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=your-email@gmail.com
EMAIL_SMTP_PASSWORD=your-gmail-app-password
EMAIL_ENABLE_SSL=true

# Gmail Setup Instructions:
# 1. Enable 2-Factor Authentication: https://myaccount.google.com/security
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password (format: xxxx xxxx xxxx xxxx)
# 4. Replace EMAIL_SMTP_USER and EMAIL_SMTP_PASSWORD with your credentials

# Alternative SMTP Providers:
# SendGrid: smtp.sendgrid.net, Port 587, User: apikey, Password: your-api-key
# Mailgun: smtp.mailgun.org, Port 587
# Outlook: smtp-mail.outlook.com, Port 587
# Office 365: smtp.office365.com, Port 587

# ========================================
# APPLICATION SETTINGS
# ========================================

# Frontend URL (used in password reset emails)
APP_CLIENT_URL=http://localhost:3000

# Backend API URL (for production deployment)
APP_API_URL=https://localhost:7000

# Environment
APP_ENVIRONMENT=Development

# ========================================
# DEFAULT USER ACCOUNTS
# ========================================

# Admin Account (Auto-seeded)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@123
ADMIN_EMAIL=admin@restaurantpos.com
ADMIN_FULLNAME=Administrator

# Staff Account (Example)
STAFF_USERNAME=staff
STAFF_PASSWORD=Staff@123
STAFF_EMAIL=staff@restaurantpos.com
STAFF_FULLNAME=Staff User

# ========================================
# FRONTEND CONFIGURATION
# ========================================
# Create a file: restaurant-pos-client/.env
# Add the following line:
# REACT_APP_API_URL=https://localhost:7000/api

# ========================================
# LOGGING CONFIGURATION
# ========================================

# Log Level Options: Trace, Debug, Information, Warning, Error, Critical
LOG_LEVEL_DEFAULT=Information
LOG_LEVEL_MICROSOFT_ASPNETCORE=Warning

# ========================================
# CORS CONFIGURATION
# ========================================

# Allowed Origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3000

# ========================================
# SECURITY SETTINGS
# ========================================

# Password Reset Token Expiry (in hours)
PASSWORD_RESET_TOKEN_EXPIRY_HOURS=1

# Account Lockout Settings
ACCOUNT_LOCKOUT_ENABLED=false
ACCOUNT_LOCKOUT_MAX_FAILED_ATTEMPTS=5
ACCOUNT_LOCKOUT_DURATION_MINUTES=15

# ========================================
# PRODUCTION DEPLOYMENT NOTES
# ========================================
# 1. NEVER commit .env files with real credentials to Git
# 2. Use Azure Key Vault, AWS Secrets Manager, or similar for production secrets
# 3. Change JWT_SECRET_KEY to a strong, random value
# 4. Use environment variables in your hosting platform (Azure, AWS, etc.)
# 5. Enable HTTPS and set APP_CLIENT_URL to your domain
# 6. Use professional email service (SendGrid, Mailgun) for production
# 7. Configure database connection string for production database
# 8. Set APP_ENVIRONMENT=Production
# 9. Review and adjust CORS_ALLOWED_ORIGINS

# ========================================
# EXAMPLE PRODUCTION .env
# ========================================
# JWT_SECRET_KEY=${AZURE_KEY_VAULT_SECRET}
# DATABASE_CONNECTION=${AZURE_SQL_CONNECTION_STRING}
# EMAIL_SMTP_PASSWORD=${SENDGRID_API_KEY}
# APP_CLIENT_URL=https://yourapp.com
# APP_ENVIRONMENT=Production
# LOG_LEVEL_DEFAULT=Warning